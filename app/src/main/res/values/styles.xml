<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_background</item>
    </style>

    <style name="ViewDivider" parent="Widget.AppCompat.TextView">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/height_1dp</item>
        <item name="android:background">@drawable/divider</item>
        <item name="android:layout_marginBottom">@dimen/margin_4dp</item>
        <item name="android:layout_marginTop">@dimen/margin_4dp</item>
    </style>


    <!--BUTTOn-->
    <style name="OvalButton" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/button_oval_selector</item>
        <item name="android:textColor">@color/colorGrey</item>
    </style>

</resources>
